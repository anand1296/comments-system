import { FilterRootCommentsPipeTsPipe } from './filter-root-comments.pipe.ts.pipe';

describe('FilterRootCommentsPipeTsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRootCommentsPipeTsPipe();
    expect(pipe).toBeTruthy();
  });
});
